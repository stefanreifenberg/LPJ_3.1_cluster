import "../../europe.ins"

searchradius 8           ! search for CRU data in up to 8 degrees around the EMDI coordinates
coordinates_precision 2  ! Use two digits after decimal point in output of coordinates
